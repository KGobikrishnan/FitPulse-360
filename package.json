{
  "name": "fitpulse-gym-management",
  "version": "1.0.0",
  "private": true,
  "description": "FitPulse 360 Gym Management System",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
